using System;

namespace _9.CicloWhile3
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Realizar un algoritmo que le pida al usuario ingresar por teclado números enteros al azar. Al usuario le corresponde ingresar la cantidad de números que va a introducir. El algoritmo debe escribir en pantalla: 1. La cantidad de números introducidos que son mayores que 0, 2. La cantidad de números introducidos que son menores que 0, 3. La cantidad de números iguales a 0.

/*            int cantidadNumeros = 0;
            int contador = 0;
            int numero = 0;
            int contadorCeros = 0;
            int contadorPositivos = 0;
            int contadorNegativos = 0;
            Console.WriteLine("Cuál es la cantidad de números a evaluar");
            cantidadNumeros=int.Parse(Console.ReadLine());

            while (contador < cantidadNumeros)
            {
                contador++;
                Console.WriteLine("Ingrese el número a evaluar");
                numero=int.Parse(Console.ReadLine());

                if (numero == 0)
                {
                    contadorCeros++;
                }
                else
                {
                    if (numero > 0)
                    { 
                        contadorPositivos++;
                    }
                    else
                    {
                        contadorNegativos++;
                    }
                }
            }
            Console.WriteLine($"De los números ingresados hay {contadorPositivos} números positivos, hay {contadorNegativos} números negativos y hay {contadorCeros} números iguales a 0.");*/

            //Escribir un algoritmo que cuente cuántos números pares hay en un rango de números del 1 al número n. (Usar el operador mod en C#).


        }
    }
}
